import React from 'react';

const Break = (props) => {
    return (
        
                <div className="bg-white btn rounded-circle p-2 " ><h2>{props.time}</h2></div>
    );
};

export default Break;